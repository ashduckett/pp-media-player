document.addEventListener('DOMContentLoaded', () => {
    // Grab the root and render the media player in it

    const root = document.getElementById('pp-media-player-root');

    const mediaPlayer = new PPMediaPlayer(root);

});